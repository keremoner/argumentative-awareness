# Python 3.13.7
# Versions are those the current results were produced with; >= is fine for
# routine work, but pin exactly if you need to reproduce stored numbers.
numpy==2.3.5
pandas==2.3.3
matplotlib==3.10.7
scipy==1.16.3
pyarrow==23.0.1

# Optional: run_sweep.py reports free RAM / RSS and uses the --min_free_gb guard
# when this is importable. The sweep runs fine without it, just without the guard.
psutil==7.1.3

# Notebooks
ipykernel==7.1.0
# Executes results/full_sweep/analyses/*.ipynb in place so their stored outputs
# stay in step with the figures they write. Minimal alternative to nbconvert.
nbclient==0.11.0

# Tests
pytest==9.0.3
